1. **Instantiate Independent Backbones & VAE Setup:** Create two separate `UNet()` objects from `unet.py` for CFDG and CFM to ensure completely independent weight initialization. Load the pre-trained VAE from `vae.py`, freeze its weights, and use `vae.encoder` to extract the deterministic mean (`mu`) of the normalized `[-1, 1]` dataset images to construct the static latent training dataset of shape `[N, 7, 14, 2]`.

2. **Shared Disentangled Label Dropout:** Implement a unified label dropout function during both training loops to force the models to learn independent representations for the left and right digits. For each batch of `[digit_left, digit_right]`, apply the following mask: 10% replace only the right digit with 9 (null), 10% replace only the left digit with 9, 10% replace both with 9 (unconditional), and 70% keep both original digits.

3. **CFDG Training Objective:** Implement a continuous-time $\epsilon$-prediction diffusion training loop. Sample time $t \sim \mathcal{U}(0, 1)$ and noise $\epsilon \sim \mathcal{N}(0, I)$. Compute the noisy latent $z_t = \sqrt{\bar{\alpha}_t} z_0 + \sqrt{1 - \bar{\alpha}_t} \epsilon$ using a standard linear noise schedule, and train the CFDG U-Net to minimize the Mean Squared Error (MSE) between its predicted noise $\epsilon_\theta(z_t, t, c)$ and the true noise $\epsilon$.

4. **CFM Training Objective:** Implement the Flow Matching training loop utilizing the Optimal Transport (OT) probability path. Sample time $t \sim \mathcal{U}(0, 1)$ and noise $z_0 \sim \mathcal{N}(0, I)$. Construct the interpolant $z_t = (1 - (1 - 10^{-5})t)z_0 + t z_1$ and the target vector field $u_t = z_1 - (1 - 10^{-5})z_0$. Train the CFM U-Net to minimize the MSE between its predicted vector field $v_\theta(z_t, t, c)$ and the target $u_t$.

5. **CFDG Sampling (DDIM/Euler):** Use an efficient DDIM sampler, functioning as a first-order Euler ODE solver, for CFDG. Initialize $z_T \sim \mathcal{N}(0, I)$ and iterate backward from $t=1$ to $0$ using 50 fixed step sizes ($\Delta t = -1/50$). Update the latent using the deterministic ODE step $z_{t+\Delta t} = z_t + \Delta t \cdot \frac{d z_t}{d t}$, calculating the derivative algebraically from the predicted noise $\epsilon_\theta$.

6. **CFM Sampling (Forward Euler):** Implement a straightforward forward Euler ODE solver to sample from the CFM model. Initialize $z_0 \sim \mathcal{N}(0, I)$ and iterate forward from $t=0$ to $t=1$ using 50 fixed step sizes ($\Delta t = 1/50$). At each step, update the latent directly using the model's predicted vector field: $z_{t+\Delta t} = z_t + \Delta t \cdot v_\theta(z_t, t, c)$.

7. **Classifier-Free Guidance Extrapolation:** Apply CFG dynamically during the numerical integration steps of both samplers. Evaluate the U-Net twice per step: once with the target labels $c$ and once with the unconditional null labels $c_{\text{null}} = [9, 9]$. Combine the outputs using the explicit extrapolation formula $Output_{guided} = (1 + w)Output_{c} - w Output_{c_{\text{null}}}$, using a fixed guidance scale such as $w=3.0$.

8. **Targeting OOD Generalization:** To accomplish the Out-of-Distribution generation task, explicitly pass the unseen label combinations ('28', '32', '53', '85', '22', '33', '55', '88') as the condition $c$ into the CFG sampling loops. The independent left/right feature extraction forced by the training dropout mask will allow the U-Nets to natively compose these novel digit pairs.

9. **Latent Space Decoding:** Once the CFDG solver reaches $t=0$ or the CFM solver reaches $t=1$, take the final converged `[B, 7, 14, 2]` latent tensors and pass them through the frozen `vae.decoder` to reconstruct the continuous image representations of shape `[B, 28, 56, 1]`.

10. **Final Image Reconstruction:** Finalize the generation pipeline by rescaling the decoder's pixel outputs from the normalized $[-1, 1]$ range back to the standard $[0, 255]$ range, casting the values to 8-bit unsigned integers (`uint8`), and saving the output OOD bi-digit images to disk.
